import { View } from "react-native";
import React from "react";

export default function ResetPasswordScreen(props) {
  return (
    <View
      style={{
        flex: 1,
        padding: 20,
        justifyContent: "center",
      }}
    >
      {/* Your Reset Password screen content goes here */}
    </View>
  );
}
